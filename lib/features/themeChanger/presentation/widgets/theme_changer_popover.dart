import 'package:flutter/material.dart';

class ThemeChangerPopover extends StatelessWidget {
  const ThemeChangerPopover({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 400,
      height: 400,
      child: Text("Popover"),
    );
  }
}
